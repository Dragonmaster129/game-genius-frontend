import React, { useState } from "react";

const Loading = (props) => {
  return (
    <div className="loading-page-wrapper">
      <div>
        <h1>Loading</h1>
      </div>
    </div>
  );
};

export default Loading;
